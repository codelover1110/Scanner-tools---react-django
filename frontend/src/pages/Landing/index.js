import React, { memo } from "react";

import LandingForm from "../../components/Landing";

import {
    Box
} from '@mui/material';

const Landing = () => {
    return (
        <Box>
            <LandingForm />
        </Box>
    );
}

export default Landing;